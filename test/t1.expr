function add (x, y) -> double
{
    (x+y) * (99 + 1);
}

function add1 (a, b) -> double
{
    a + b;
}

function mul (n, m) -> double
{
   n * m;
}

function mul2 (c, v) -> double
{
    c * v;
}

function mul3 (q, w) -> double
{
    q * w;
}

function mul4 (e,r,t,z) -> double
{
    e * r * t * z;
}

function main () -> double
{
    6/3;
    1+2+3;
    var count:double = (1 + 2 + 3 + 4) + 100 * 100 / 2 * 3 + (1000 + 22 + 55 +77) + 10.765;
    var average:double = (1 + 2 + 3)/3;
    count = 0;
    count = count + 1;
    count = count + 1;
    count = count + 1;
    count = count + 1;
    count = count + 1;
    add(1,2);
    count = count + add(1,3);
    mul(1024,4);
    mul2(1024,4);
    var rimini:double = mul(mul2(2,8),mul3(2,8));
    rimini = rimini + 111;
    add(100, add1(111, mul(mul2(2,8),mul3(2,8))));
    mul4(1,2,3,4);
}
